# 系统代号，用于部署子路径（对应 vite.config.ts 中的 base）
VITE_SYS_CODE=hrs

# 主系统 API 基础路径
VITE_API_BASE_URL=/api

# HRS v1 契约接口（阶段 0 起统一使用）
VITE_HRS_API_BASE_URL=/api/hrs/v1

# GitHub Pages 等不支持 SPA 回退的静态托管使用 hash；正式服务器默认 history
VITE_ROUTER_MODE=history

# 开发阶段默认使用本地 Mock，联调时改为 false
VITE_SWITCH_MOCK=true
